# Site canonical URL (used for canonical tags, sitemap, OG).
SITE_URL=https://example.com

# Which Decap CMS backend public/admin/config.yml is built for.
# Values: local | netlify | github
# After changing, run:  pnpm cms:switch   (build runs it automatically via prebuild)
CMS_BACKEND=local
